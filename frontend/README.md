# Employment History Frontend

Angular frontend application for managing employment history.

## Setup

1. Install dependencies:
```bash
npm install
```

2. Start the development server:
```bash
npm start
```

The application will be available at `http://localhost:5003`

Make sure the backend server is running on `http://localhost:5005` for the API to work.

